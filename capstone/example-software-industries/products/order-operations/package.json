{
  "name": "@esi/order-operations-capstone",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs ../../legacy/operations-desk-classic/tests/*.test.mjs",
    "test:legacy": "node --test ../../legacy/operations-desk-classic/tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
